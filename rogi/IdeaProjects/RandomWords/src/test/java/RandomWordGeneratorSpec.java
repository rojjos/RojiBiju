import org.junit.Before;
import org.junit.Test;
import static org.junit.Assert.*;

/**
 * Created by rogi on 1/21/16.
 */
public class RandomWordGeneratorSpec {
    //

    //@Before // this should test first before any other tests
    //public void constructor(){
    // randomWordGenerator = new RandomWordGenerator();
    //}


    @Test
    public void makeWordTest() {

        String requestedLetter = "d";
       /* String randomWord = RandomWordGenerator.makeWord(requestedLetter.charAt(0));
        assertNotNull("break the test by returning null but expecting a String value", randomWord);
        assertEquals("requestedLetter should be equals to randomWord", randomWord.charAt(0), requestedLetter.charAt(0));
*/

    }
}